# glass_dag

A Python project for modeling Directed Acyclic Graphs (DAGs).

## Installation

```bash
pip install -r requirements.txt
```

## Usage

Import and use the `dag_model` module from the `glass_dag` package.

## Testing

```bash
pytest
```
